# Maven build output
target/
*.jar
*.war
*.class

# IDE files — don't commit these, they're machine-specific
.idea/
*.iml
.vscode/
*.eclipse
.project
.classpath
.settings/

# OS garbage
.DS_Store
Thumbs.db

# Spring Boot dev tools
spring-pid

# Docker volumes (if you ever mount locally)
postgres_data/
redis_data/

# Sensitive config — if you ever add secrets locally
application-local.yml
application-secret.yml
*.env
